<h1>Rockin' Burgers!</h1>

<h2>Rockin' burger ready for grub</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Devoured Rockin' Burgers!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Rockin' Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burg">Rockin' Burger Name:</label>
    <input type="text" id="burg" name="burger_name">
  </div>

  <div class="form-group">
    <label for="devoured">Devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> Ready to be devoured!<br>
    <input type="radio" name="devoured" value="0"> Devoured
  </div>

  <button type="submit">Add Rockin' Burger</button>
</form>